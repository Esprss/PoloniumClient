package polonium.client.features.modules;
public class FOV extends Module {
    public FOV() {
        super("FOV", Category.RENDER); // Предполагаем RENDER, но это не имеет значения для компиляции
    }
}
